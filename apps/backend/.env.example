# =============================
# 基础运行环境
# =============================
# development / test / production
NODE_ENV=development

# 监听地址与端口
HOST=0.0.0.0
PORT=4000

# =============================
# MySQL 数据库
# =============================
MYSQL_HOST=example.mysql.local
MYSQL_PORT=3306
MYSQL_USER=auto-ssl
MYSQL_PASSWORD=change-me
MYSQL_DATABASE=auto_ssl

# =============================
# 邮件 / SMTP（用于验证码、通知等）
# =============================
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_USER=no_reply@example.com
SMTP_PASSWORD=change-me

# 发件人邮箱（必须是合法邮箱格式）
FROM_EMAIL=no_reply@example.com

# =============================
# Web 控制台地址（邮件里的验证链接会用到）
# =============================
WEB_BASE_URL=http://localhost:5173

# =============================
# JWT / 鉴权
# =============================
# 至少 16 位，建议使用随机长字符串
JWT_SECRET=change-me-please-32-bytes

# 可选：Access/Refresh token 过期时间（不填会使用默认值）
ACCESS_TOKEN_TTL_MINUTES=15
REFRESH_TOKEN_TTL_DAYS=30

# =============================
# 数据加密（AES-256-GCM）
# =============================
# 用于加密存储敏感配置（例如云厂商密钥）。建议使用 32 字节随机字符串
DATA_ENCRYPTION_KEY=change-me-please

# =============================
# ACME / Let's Encrypt
# =============================
# 可选：用于注册 ACME 账号（建议填写）
ACME_ACCOUNT_EMAIL=you@yourdomain.com

# ACME 目录地址：开发建议先用 staging，避免触发生产限额
# staging: https://acme-staging-v02.api.letsencrypt.org/directory
# production: https://acme-v02.api.letsencrypt.org/directory
ACME_DIRECTORY_URL=https://acme-v02.api.letsencrypt.org/directory

# HTTP-01 挑战（使用 DNS-01 时可不填 ACME_HTTP_HOST）
# 对外可访问的域名 / 公网 IP（可选）
ACME_HTTP_HOST=your-domain.com
ACME_HTTP_PORT=80

# 某些本地/内网调试场景可临时跳过本地可达性验证（true/false）
ACME_SKIP_LOCAL_VERIFY=false

# DNS-01 等待与 TTL（根据 DNS 服务商生效时间调整）
ACME_DNS_WAIT_SECONDS=20
ACME_DNS_TTL=600

# =============================
# 自动续签 / 调度
# =============================
# 证书到期前多少天开始续签（默认 30）
RENEWAL_THRESHOLD_DAYS=30

# cron 表达式：默认每天 03:00 执行（示例：0 3 * * *）
CRON_SCHEDULE=0 3 * * *
